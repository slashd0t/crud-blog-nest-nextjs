import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /* keep Next config options here (remove ESLint-only keys like `extends`) */
};

export default nextConfig;
